spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/hello_server?useUnicode=true&characterEncoding=utf8&serverTimezone=AsiaShanghai&useSSL=false
    username: root
    password: 123456

  # 正确 AI 配置（我帮你修正）
  ai:
    alibaba:
      api-key: sk-84a291399dd04712a40004f196d8c6e4
      chat:
        options:
          model: qwen-turbo

  # Redis 正常启用
  redis:
    host: localhost
    port: 6379

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

security:
  jwt:
    secret: your-secret-key-2026-fwj-hello-server
    expiration-millis: 7200000